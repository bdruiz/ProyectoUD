{% extends '::base.html.twig' %}

{% block body -%}
    <h1>InventarioDeportes list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fecha</th>
                <th>Estado</th>
                <th>Cantidad</th>
                <th>Elemento</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('ud_deportes_inventario_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.estado }}</td>
                <td>{{ entity.cantidad }}</td>
                <td>{{ entity.elemendepor._toString }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('ud_deportes_inventario_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('ud_deportes_inventario_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('ud_deportes_inventario_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
