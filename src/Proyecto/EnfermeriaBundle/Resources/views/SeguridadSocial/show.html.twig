{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SeguridadSocial</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Eps</th>
                <td>{{ entity.eps }}</td>
            </tr>
            <tr>
                <th>Rh</th>
                <td>{{ entity.rh }}</td>
            </tr>
            <tr>
                <th>Telefonocontacto</th>
                <td>{{ entity.telefonoContacto }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('ud_enfermeria_ssocial') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('ud_enfermeria_ssocial_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
